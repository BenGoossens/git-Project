@model IEnumerable<Project_Krekelhof.ViewModels.UitleningIndexViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EindeUitlening)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartUitlening)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsTerug)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EindeUitlening)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartUitlening)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsTerug)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
